{
  "name": "Generate Team Contact Sheets",
  "nodes": [
    {
      "parameters": {
        "path": "generate-team-contact-sheets",
        "httpMethod": "POST",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "r5000000-0000-0000-0000-000000000001",
      "name": "Generate Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        220,
        140
      ],
      "webhookId": "generate-team-contact-sheets"
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const input = $input.first()?.json ?? {};\nconst clean = (value) => value === null || value === undefined ? '' : String(value).trim();\nconst parseObject = (value) => {\n  if (typeof value === 'string') {\n    try {\n      const parsed = JSON.parse(value);\n      return parsed && typeof parsed === 'object' && !Array.isArray(parsed) ? parsed : {};\n    } catch {\n      return {};\n    }\n  }\n  return value && typeof value === 'object' && !Array.isArray(value) ? value : {};\n};\nconst pick = (...values) => values.find((value) => clean(value) !== '');\nconst parseOptionalBool = (value) => {\n  const normalized = clean(value).toLowerCase();\n  if (!normalized) return null;\n  if (['1', 'true', 'yes', 'y', 'on'].includes(normalized)) return true;\n  if (['0', 'false', 'no', 'n', 'off'].includes(normalized)) return false;\n  return null;\n};\nconst normalizeAttachmentMode = (value) => {\n  const normalized = clean(value).toLowerCase().replace(/_/g, '-');\n  const aliases = {\n    '1': 'own-plus-reserves',\n    '2': 'own-next-plus-reserves',\n    '3': 'all-in-section',\n    'own-plus-reserves': 'own-plus-reserves',\n    'ownplusreserves': 'own-plus-reserves',\n    'own+reserves': 'own-plus-reserves',\n    'own-reserves': 'own-plus-reserves',\n    'own-next-plus-reserves': 'own-next-plus-reserves',\n    'ownnextplusreserves': 'own-next-plus-reserves',\n    'own+next+reserves': 'own-next-plus-reserves',\n    'own-next-reserves': 'own-next-plus-reserves',\n    'ownnextreserves': 'own-next-plus-reserves',\n    'own-next': 'own-next-plus-reserves',\n    'current': 'own-next-plus-reserves',\n    'own-next-plus-reserve': 'own-next-plus-reserves',\n    'own_next_reserves': 'own-next-plus-reserves',\n    'all-in-section': 'all-in-section',\n    'allinsection': 'all-in-section',\n    'all': 'all-in-section',\n  };\n  return aliases[normalized] || 'all-in-section';\n};\nconst body = parseObject(input.body);\nconst query = parseObject(input.query);\nconst params = parseObject(input.params);\nconst season = clean(pick(body.season, input.season, query.season, params.season)) || '2026';\nconst category = clean(pick(body.category, input.category, query.category, params.category)) || 'ALL';\nconst attachmentMode = normalizeAttachmentMode(\n  pick(\n    body.attachment_mode,\n    body.attachmentMode,\n    body.scope,\n    input.attachment_mode,\n    input.attachmentMode,\n    input.scope,\n    query.attachment_mode,\n    query.attachmentMode,\n    query.scope,\n    params.attachment_mode,\n    params.attachmentMode,\n    params.scope,\n  ) || 'all-in-section'\n);\nconst modeCandidates = [body.delivery_mode, body.deliveryMode, input.delivery_mode, input.deliveryMode, query.delivery_mode, query.deliveryMode, query.mode, params.delivery_mode, params.deliveryMode, params.mode];\nconst normalizedMode = String(modeCandidates.find((value) => clean(value) !== '') ?? 'test').trim().toLowerCase();\nconst deliveryMode = normalizedMode === 'production' ? 'production' : 'test';\nconst testRecipient = clean(pick(body.test_recipient, body.testRecipient, input.test_recipient, input.testRecipient, query.test_recipient, query.testRecipient, params.test_recipient, params.testRecipient)) || 'steve.short@gmail.com';\nconst listOnly = parseOptionalBool(pick(body.list_only, body.listOnly, input.list_only, input.listOnly, query.list_only, query.listOnly, params.list_only, params.listOnly)) === true;\nconst generateOnly = parseOptionalBool(pick(body.generate_only, body.generateOnly, input.generate_only, input.generateOnly, query.generate_only, query.generateOnly, params.generate_only, params.generateOnly)) === true;\nconst skipSyncFlag = parseOptionalBool(pick(body.skip_sync, body.skipSync, input.skip_sync, input.skipSync, query.skip_sync, query.skipSync, params.skip_sync, params.skipSync));\nconst skipTriggerFlag = parseOptionalBool(pick(body.skip_trigger, body.skipTrigger, body.skip_send, body.skipSend, input.skip_trigger, input.skipTrigger, query.skip_trigger, query.skipTrigger, params.skip_trigger, params.skipTrigger));\nconst syncRequested = parseOptionalBool(pick(body.sync, body.run_sync, body.runSync, input.sync, input.run_sync, query.sync, query.run_sync, params.sync, params.run_sync));\nconst triggerRequested = parseOptionalBool(pick(body.trigger, body.send, body.run_trigger, body.runTrigger, input.trigger, input.send, query.trigger, query.send, params.trigger, params.send));\nlet skipSync = skipSyncFlag ?? !(syncRequested === true || triggerRequested === true);\nlet skipTrigger = skipTriggerFlag ?? (triggerRequested === true ? false : true);\nif (triggerRequested === true && skipSyncFlag !== true) {\n  skipSync = false;\n}\nif (listOnly || generateOnly) {\n  skipSync = true;\n  skipTrigger = true;\n}\nreturn [{ json: { season, category_requested: category, attachment_mode: attachmentMode, delivery_mode: deliveryMode, test_recipient: testRecipient, list_only: listOnly, generate_only: generateOnly, skip_sync: skipSync, skip_trigger: skipTrigger, teams_dir: '/mnt/c/dev/avondale-n8n/Teams', output_dir: '/mnt/c/dev/avondale-n8n/Teams/generated', manifest_path: '/mnt/c/dev/avondale-n8n/Teams/generated/team-captain-email-jobs.json', review_path: '/mnt/c/dev/avondale-n8n/Teams/generated/CAPTAIN_EMAIL_SEND_LIST.md', base_dir: clean(pick(body.base_dir, input.base_dir, query.base_dir, params.base_dir)) || '/home/node/.n8n-files/teams-mailout/current', webhook_base_url: clean(pick(body.webhook_base_url, input.webhook_base_url, query.webhook_base_url, params.webhook_base_url)) || 'http://127.0.0.1:5678' } }];"
      },
      "id": "r5000000-0000-0000-0000-000000000002",
      "name": "Resolve Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        480,
        140
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const input = $('Resolve Input').first().json ?? {};\nreturn [{\n  json: {\n    ...input,\n    generator_command: '/mnt/c/dev/postgres-mcp-venv-linux/bin/python',\n    generator_args: ['/mnt/c/dev/avondale-n8n/Teams/generate_team_contact_lists.py'],\n    env: {\n      TEAM_CONTACTS_SEASON: input.season,\n      TEAM_CAPTAIN_ATTACHMENT_MODE: input.attachment_mode,\n    },\n  }\n}];"
      },
      "id": "r5000000-0000-0000-0000-000000000003",
      "name": "Build Command",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        740,
        140
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "const input = $('Build Command').first().json ?? {};\nconst { execFileSync } = require('child_process');\nconst fs = require('fs');\nconst clean = (value) => value === null || value === undefined ? '' : String(value).trim();\nconst parseJson = (value) => {\n  try {\n    return JSON.parse(value);\n  } catch {\n    return null;\n  }\n};\ntry {\n  const generatorOutput = execFileSync(input.generator_command, input.generator_args, {\n    env: { ...process.env, ...(input.env || {}) },\n    cwd: input.teams_dir,\n    encoding: 'utf8',\n    timeout: 240000,\n  }).trim();\n\n  if (input.list_only) {\n    const reviewMarkdown = fs.readFileSync(input.review_path, 'utf8');\n    return [{ json: { success: true, season: input.season, attachment_mode: input.attachment_mode, message: reviewMarkdown } }];\n  }\n\n  const manifest = JSON.parse(fs.readFileSync(input.manifest_path, 'utf8'));\n  const messageLines = [\n    `Generated team contact sheets for season ${input.season}.`,\n    `Attachment mode: ${input.attachment_mode}.`,\n  ];\n  if (clean(input.category_requested) && clean(input.category_requested).toUpperCase() !== 'ALL') {\n    messageLines.push(`Requested category: ${input.category_requested} (generator still processes all source documents, matching the local script).`);\n  }\n  if (generatorOutput) {\n    messageLines.push(generatorOutput);\n  }\n\n  if (!input.skip_sync) {\n    const syncOutput = execFileSync('bash', ['/mnt/c/dev/avondale-n8n/scripts/sync-team-captain-mailout-to-n8n.sh'], {\n      cwd: '/mnt/c/dev/avondale-n8n',\n      encoding: 'utf8',\n      timeout: 120000,\n    }).trim();\n    if (syncOutput) {\n      messageLines.push(syncOutput);\n    }\n  }\n\n  if (!input.skip_trigger) {\n    const payload = {\n      delivery_mode: input.delivery_mode,\n      test_recipient: input.test_recipient,\n      base_dir: input.base_dir,\n      attachment_mode: manifest.attachment_mode || input.attachment_mode,\n      jobs: Array.isArray(manifest.jobs) ? manifest.jobs : [],\n    };\n    const rawResponse = execFileSync('curl', [\n      '-sS',\n      '-w',\n      '\\n%{http_code}',\n      '-H',\n      'Content-Type: application/json',\n      '--data-binary',\n      JSON.stringify(payload),\n      `${String(input.webhook_base_url).replace(/\\/$/, '')}/webhook/send-team-captain-contact-lists`,\n    ], {\n      encoding: 'utf8',\n      timeout: 300000,\n    }).trimEnd();\n    const responseLines = rawResponse.split('\\n');\n    const statusLine = responseLines.pop() || '';\n    const responseBody = responseLines.join('\\n').trim();\n    const statusCode = Number.parseInt(statusLine, 10);\n    if (!Number.isInteger(statusCode) || statusCode < 200 || statusCode >= 300) {\n      throw new Error(`Send Team Captain Contact Lists failed (${statusLine || 'unknown'}): ${responseBody || 'No response body'}`);\n    }\n    const parsedBody = parseJson(responseBody);\n    if (parsedBody && typeof parsedBody === 'object' && clean(parsedBody.message)) {\n      messageLines.push(`Send webhook: ${parsedBody.message}`);\n    } else if (responseBody) {\n      messageLines.push(`Send webhook response: ${responseBody}`);\n    } else {\n      messageLines.push(`Send webhook triggered in ${input.delivery_mode} mode.`);\n    }\n  }\n\n  return [{ json: { success: true, season: input.season, attachment_mode: input.attachment_mode, message: messageLines.join('\\n') } }];\n} catch (error) {\n  const parts = [clean(error.message) || 'Unknown error'];\n  const stdout = clean(error.stdout);\n  const stderr = clean(error.stderr);\n  if (stdout) parts.push(stdout);\n  if (stderr) parts.push(stderr);\n  return [{ json: { success: false, error: parts.filter(Boolean).join('\\n') } }];\n}"
      },
      "id": "r5000000-0000-0000-0000-000000000004",
      "name": "Run Generator",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1000,
        140
      ]
    },
    {
      "parameters": {
        "respondWith": "text",
        "responseBody": "={{ $json.success ? $json.message : 'Error: ' + $json.error }}",
        "options": {
          "responseCode": "={{ $json.success ? 200 : 500 }}",
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "text/plain"
              }
            ]
          }
        }
      },
      "id": "r5000000-0000-0000-0000-000000000005",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        1260,
        140
      ]
    }
  ],
  "connections": {
    "Generate Webhook": {
      "main": [
        [
          {
            "node": "Resolve Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Resolve Input": {
      "main": [
        [
          {
            "node": "Build Command",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Command": {
      "main": [
        [
          {
            "node": "Run Generator",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Generator": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "availableInMCP": false
  }
}
